{
  "command-runner.commands": {
    "run": "conda activate autonet && python main.py",
    "run test": "conda activate autonet && python test.py",
    "install requirements": "conda activate autonet && pip install -r requirements.txt"
  }
}
